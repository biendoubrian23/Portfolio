# Ce fichier n'est pas utilisé - le portfolio est sur Vercel
# La redirection ads.txt est dans next.config.ts

[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
